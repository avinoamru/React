import React from "react";

const Sets = () => (
  <section>
    <h3>Set</h3>

    <p>
      A set is a collection which is unordered and unindexed. In Python sets are
      written with curly brackets. Example Create a Set: thisset ={" "}
      {("apple", "banana", "cherry")} print(thisset)
    </p>
  </section>
);

export default Sets;
