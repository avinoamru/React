import React from "react";

const Numbers = () => (
  <section>
    <h3>Python Numbers</h3>
    <p>
      There are three numeric types in Python: int float complex Variables of
      numeric types are created when you assign a value to them: Example x = 1 #
      int y = 2.8 # float z = 1j # complex
    </p>
  </section>
);

export default Numbers;
